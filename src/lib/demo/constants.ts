/**
 * Shared identifiers for demo mode — kept in one place so the seed script and
 * the auth bypass (`getOptionalAuth`) agree on how to find "the" demo user
 * without a real session cookie.
 */
export const DEMO_TENANT_SLUG = "demo";
export const DEMO_USER_EMAIL = "demo@newwin.dev";
